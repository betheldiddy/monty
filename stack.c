#include "monty.h"
/**
 * f_stack - prints at the top
 * @head: stack heads
 * @counter: line numbers
 * Return: return none
*/
void f_stack(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	bus.lifi = 0;
}
